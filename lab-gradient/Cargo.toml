[package]
name = "lab-gradient"
version = "0.1.0"
edition = "2024"

[dependencies]
image = { version = "0.25", default-features = false, features = ["jpeg", "png", "rayon"] }
palette = "0.7"
anyhow = "1"
