[package]
name = "residual-viz"
version = "0.1.0"
edition = "2024"

[dependencies]
image = { version = "0.25", default-features = false, features = ["jpeg", "png", "rayon"] }
palette = "0.7"
rayon = "1"
anyhow = "1"
